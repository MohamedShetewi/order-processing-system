# Placeholder secret — replace values before applying, or generate with:
#   kubectl create secret generic order-processing-secrets \
#     --from-literal=DB_PASSWORD=<db-password> \
#     --from-literal=REDIS_PASSWORD=<redis-password>
apiVersion: v1
kind: Secret
metadata:
  name: order-processing-secrets
type: Opaque
stringData:
  DB_PASSWORD: password
  REDIS_PASSWORD: ""
